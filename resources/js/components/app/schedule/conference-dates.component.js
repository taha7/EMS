// import React, { memo } from "react";
// import { connect } from "react-redux";

// const ConferenceDates = memo(({ dates }) => {
//     console.log("rerender dates");

//     return dates.map(date => (
//         <th className="dates-title " key={date}>
//             {date}
//         </th>
//     ));
// });

// const mapStateToProps = ({ datesWidthReducer }) => ({
//     datesWidth: datesWidthReducer
// });

// export default connect(mapStateToProps, null)(ConferenceDates);
